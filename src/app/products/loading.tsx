const LoadingProducts = () => {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        height: "100vh",
      }}
    >
      <h2>Loading Products...</h2>
    </div>
  );
};

export default LoadingProducts;
